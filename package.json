{
  "name": "qissat-qabila",
  "version": "1.1.0",
  "description": "قصة قبيلة - Arabic Web Novel",
  "scripts": {
    "dev": "npx serve .",
    "build": "tailwindcss -i ./src/input.css -o ./styles.css --minify",
    "deploy": "npm run build && git add . && git commit -m 'update' && git push"
  },
  "devDependencies": {
    "tailwindcss": "^3.4"
  }
}
